{
  "REGEX": "CAST(%s AS CHAR) REGEXP ?",
  "LIKE": "CAST(%s AS CHAR) LIKE ?",
  "EQUAL": "CAST(%s AS CHAR) = ?",
  "GREATER_NUMERIC": "CAST(%s AS DECIMAL) > ?",
  "GREATER_DATE": "CAST(%s AS DATE) > CAST(? AS DATE)",
  "RANGE_NUMERIC": "CAST(%s AS DECIMAL) BETWEEN ? AND ?"
}